export enum USER_ROLE {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export const UNEXPECTED_ERROR: string = 'Ocurrio un problema inesperado';

export enum SUCCESSFULL_MESSAGE {
  CRETED = 'Usuario creado!',
}

export enum ERROR_MESSAGE {
  NOT_FOUND = 'El usuario no fue encontrado',
  UNEXPECTED = 'Error inesperado',
}
